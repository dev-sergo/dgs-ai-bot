# 05 — Дорожная карта качества и безопасности

Живой документ. Ведём по нему доработки **от простого к сложному** и фиксируем **каждую итерацию**,
чтобы всегда было видно: где мы сейчас, что делали в прошлый раз, что дальше.

## Как пользоваться

1. Берём следующую невыполненную задачу из чек-листа (сверху вниз — порядок = приоритет).
2. Делаем, прогоняем тесты/бенчмарк.
3. **Дописываем строку в «Журнал итераций»** (сверху, новые — выше): дата, что сделали, результат.
4. Отмечаем задачу `[x]` и ставим дату/коммит.

Метрика прогресса — pass-rate бенчмарка `make eval-host` (см. [03-testing-strategy.md](03-testing-strategy.md)).
Текущий baseline зафиксирован ниже.

---

## Журнал итераций

> Новые записи — сверху. Формат: **дата — что сделали — результат / следующий шаг**.

- **2026-06-23 — Консультант: advice-eval + расширение bundle (AvgCheck/ChannelMix/TopProducts). Риг 86/86.**
  Две итерации одним коммитом (`8812017`). **(1) Advice-eval (must-mention).** `pipeval.Expect` получил
  `contains_any` (хотя бы одна подстрока) и `mentions_number` (цифра в тексте, `digitRe`) — регрессионная
  защита совета без LLM-judge. 3 advice-кейса в quality.jsonl усилены ассертами против детерминированного
  снимка (возвраты есть только в мае → кейс «за прошлый месяц» их ловит ≈540.09; скидки/аутсайдеры из
  products не фильтруются по периоду → всегда в bundle). Юнит-тесты `pipeval_test.go`. **(2) Расширение
  bundle.** В `InsightBundle` добавлены `AvgCheck` (взвешенный средний чек sum_all/kol_vo_chekov, НЕ наивное
  среднее строк), `ChannelMix` (ненулевые каналы оплаты по убыванию суммы + доля) и `TopProducts` (лидеры по
  выручке). `bottomProducts` обобщён в `productsByAmount(rows, n, asc)`. `Compose` рендерит новые секции
  (средний чек с дельтой, расклад каналов, «Ключевые позиции»). Юнит-тесты на AvgCheck/ChannelMix + кейс
  «взвешенное vs наивное». **Прогон на риге** (`pipeval-quality-host`, qwen2.5-32b): **86/86 PASS, 0 ERR**,
  все советы на русском, числа grounded, безопасность/off_topic держатся. **КЛЮЧЕВАЯ НАХОДКА: новые поля
  bundle (avg_check/channel_mix/top_products) НЕ используются.** Они физически уезжают в модель (`json.Marshal`
  всей структуры), но `systemPrompt` advisor'а описывает только revenue/returns_sum/discounts/bottom_products
  → модель их игнорирует. Eval прошёл, потому что ассерты проверяют драйверы потерь + числа (старые поля).
  Особенно заметно на «что улучшить»: модель говорит общими словами про «падение спроса», хотя в bundle
  лежат точные средний чек и каналы. **Математика+тесты+eval зелёные, регрессии нет, но ценность расширения
  пока нулевая в тексте.** Следующий шаг — дописать advisor-`systemPrompt` под новые поля (правка промпта →
  полный eval-host по правилу) + усилить advice-ассерты на упоминание чека/канала. Дальше: фильтры
  (точка/категория) в снимке.

- **2026-06-23 — Консультант: тонкий вертикальный срез (advice-режим). Детерминированная часть готова.**
  Первый сквозной срез главной фичи — **advisor**. Запросы «на чём теряю / что убрать из меню / как
  поднять выручку / что улучшить» раньше падали в clarify/off_topic, теперь идут в отдельный режим:
  детерминированный снимок бизнеса + LLM-консультант поверх чисел. **Ядро:** `engine.BuildInsightBundle`
  (выручка+тренд, возвраты, скидки, аутсайдеры меню) — чистая функция над выборками payment(now/prev)+
  products(now), переиспользует хелперы движка (7aa8037). **Маршрутизация:** `RefineAdvice` —
  детерминированный узкий guard (intent=advice), generic «совет по бизнесу» остаётся off_topic. **Advisor:**
  `internal/advisor` — LLM с жёстким grounding (только числа, только русский) + детерминированный `Compose`
  как fallback и stub-режим; `hasNonRussian` ловит срыв qwen в китайский (баг 5.5). **Оркестрация:**
  `app.advise` собирает снимок из нескольких Fetch; пустой период → clarify, нет данных → честный текст
  (833fa53). Дизайн обновлён в [06-assistant-design.md](06-assistant-design.md). Юнит+интеграционные тесты
  зелёные, pipeval 17/17. Детерминированный совет проверен на фикстурах (читается осмысленно: называет
  скидки/аутсайдеры с числами). **Остаток: LLM-advisor промпт ждёт `pipeval-quality-host` на риге**
  (как было с нарратором — eval-host advisor не покрывает). Дальше: derived-метрики, ChannelMix/TopProducts
  в bundle, advice-eval, фильтры в снимке.

- **2026-06-23 — Резолвер на живые UUID + 2 бага планировщика/нарратора. eval-host 518/551, quality 82/82.**
  **п.1 (Резолвер на живые UUID):** `resolver.NewLiveStore` строит справочник из `<select>`-элементов
  HTML-формы отчёта payment (`HTMLClient.FetchSelects` парсит `BaseReportForm[locality_id]/[sale_point_id]`
  → `{UUID, Name}`). При `DGS_CLIENT=http` UUID берутся живыми из разметки, при ошибке — fallback на
  grid-фикстуры; `DGS_CLIENT=fixture` (CI/eval) остаётся полностью детерминированным. Коммит `3ff40b9`.
  **Баг A (`tip_oplaty`/payment_type на payment):** `RefinePaymentChannelFilter` снимает невалидный фильтр
  `payment_type` с отчёта payment и переводит распознанный канал в колонку (card→sum_card, …) + group_by=date;
  для compare/contribution метрику не трогает. Follow-up «а по карте?» больше не падает в out_of_scope
  (подтверждено в pipeval-quality). Коммит `4689b18`. **Баг B (нарратор путал sredniy_chek/выручку):** движок
  кладёт человекочитаемый `metric_label` из каталога в `Meta` (compare/contribution), нарратор передаёт его
  как `metric` и промпт запрещает подменять метрику. «почему упала выручка» → нарратив говорит «выручка»,
  не «средний чек». Коммит `40fd866`. Юнит-тесты на parseSelects/NewLiveStore/RefinePaymentChannelFilter,
  +2 кейса в quality.jsonl. **eval-host 518/551 (+13 к baseline 505, без регрессии), pipeval-quality 82/82, 0 ERR.**
  **Найден новый баг (заведён в Этапе 5.5, backlog):** LLM-нарратор (qwen2.5) иногда срывается в КИТАЙСКИЙ
  в нарративе (кейс «а за прошлый?»: `Прирост выручки主要是由信用卡支付增加…`). Числа/таблица верны, кейс PASS,
  но текст нечитаем. Следующий трек — **Авто-рефреш куки (п.2)** или **мультитенантность (п.3)**.

- **2026-06-22 — ERR/L/I: period-токены, таблица contribution prev=0, follow-up method=plain. quality-run7: 80/80, 0 ERR.**
  **ERR:** новые токены периода (`last_14_days`, `last_week`, `last_90_days`, `last_3_months`); `ErrUnknownToken` ловится
  в `app.Ask` и возвращает clarify вместо 500 (проверено: «позапрошлая неделя» → PASS). **L:** при `vPrev=0`
  колонка «Доля изменения» в таблице contribution скрывается через мета-флаг `empty_prev` — таблица больше
  не противоречит нарративу (показывает только Компонента/Изменение/Текущий/Предыдущий). **I:** `RefineDefaultMethod`
  выставляет `method=plain` при пустом method + заданном периоде — follow-up `«а по карте?»` теперь доходит
  до валидатора с `method=plain`, но валидатор всё равно отклоняет (модель добавляет невалидный фильтр
  `tip_oplaty`). `method=""` как причина out_of_scope устранена, но симптом сохраняется — остаток в backlog.
  Попутно убраны неиспользуемые импорты в `transport/http/server.go`. Промпт планировщика не трогался →
  `eval-host` не нужен. Следующий трек — **Консультант** (Insight-bundle + advisor-промпт, ~1 нед MVP).

- **2026-06-22 — B: честный отказ на рейтинг по сотрудникам + нарратив при пустом предыдущем периоде.**
  **B (остаток прошлой итерации) закрыт:** «топ продавцов / лучшие официанты / рейтинг кассиров / какой
  оператор обработал больше всего чеков» модель молча отдавала топ ТОВАРОВ (измерения «сотрудник» в каталоге
  нет). Детерминированный guard `RefineEmployeeRanking` (rank-слово И обозначение человека, любой порядок;
  имя собственное после роли → НЕ трогаем, это легальный фильтр «чеки сотрудника Иванова») помечает план
  `off_topic` с честным текстом «Рейтинг по сотрудникам пока недоступен…». Маршрутизация детерминированная,
  не через промпт (урок прошлых итераций). **Нарратив при value_prev=0:** в промпт LLM-нарратора добавлено
  правило — не считать проценты/доли при пустом предыдущем периоде («+100%»/«доля 119%» — бессмыслица),
  писать «сравнение неполное» (шаблонный `Compose` это уже умел). Юнит-тесты на regex (ловит рейтинги,
  не задевает имя/рейтинг товаров), `quality.jsonl` +7 кейсов (B-варианты + prev=0). Сборка/vet/тесты зелёные;
  полный eval-host не требовался (общий промпт планировщика не тронут). Следующий крупный трек — **Консультант**
  (надстройка над нарратором: Insight-bundle + advisor-промпт, ~1 нед MVP).

- **2026-06-22 — Большой качественный срез (74 кейса) + честный отказ на непримененный фильтр.**
  Расширен `quality.jsonl` до 74 кейсов (бизнес/рейтинги/аналитика/советы/диалоги/фильтры/офф-топик/
  джейлбрейк/опечатки), прогон с дампом ответов (quality-run5.txt). Ядро крепкое: числа/периоды/каналы
  верны, рейтинги лучшие↔худшие, безопасность 100% (все джейлбрейки/PII/мутации→off_topic), опечатки и
  follow-up отрабатывают. **Главная находка — молчаливо-неверные ответы на неподдержанные разрезы**:
  «выручка по точке Выкса»/«в Москве» показывали ПОЛНЫЙ отчёт под видом отфильтрованного (фильтр строился,
  но у отчёта нет колонки → `filters_skipped` молча). **Фикс A:** при непустом `FiltersSkipped` — честный
  отказ с именем разреза (точка/город/категория/…), а не данные. Остаток: **B** «топ продавцов» (модель не
  строит фильтр вовсе → отдаёт products; нужен узкий guard или измерение-сотрудник, Этап 5.4); причинные
  нарративы врут при пустом предыдущем периоде («+100%»). **Стратегия: заказчику нужен планировщик+КОНСУЛЬТАНТ;
  фундамент — нарратор уже есть, консультант = надстройка (derived-метрики 5.1 → MVP-советник, ~2 нед).**

- **2026-06-22 — Полировка качества G+H+D.** G: `sortKeyFor` без явного `sort_by` ранжирует top_n по
  денежной метрике (выручке), а не по количеству — «топ товаров» больше не выносит наверх позиции с 0₽.
  H: убрана пустая строка «Итого:» в contribution. **D оказался РЕАЛЬНЫМ багом** (не шумом): `primaryMetric`
  для compare/contribution брал `Metrics[0]`, и когда модель ставила первым измерение `date` — суммировалась
  дата → 0 → ложное «данных нет»; плавало от прогона к прогону по порядку метрик. Фикс: предпочитать RUB-поле
  (тот же приём, что в G). Все три верифицированы на реальной LLM (quality-run4) + интеграционные тесты.
  Остаток: **E** «сколько чеков» (счётчик-итог), **F** «средний чек за месяц» (взвешенное среднее) —
  derived-метрики (Этап 5.1), требуют движка. Все реальные баги из разбора качества (A/B/C/D/G/H) закрыты.

- **2026-06-22 — Качественный прогон по ОТВЕТАМ (pipeval --dump) + фиксы.** Добавлен режим `PIPEVAL_DUMP`
  (`make pipeval-quality-host`) — печатает план + отрендеренный текст ответа по набору `quality.jsonl`.
  Вскрыл то, что планировщик-eval (только план) не видит. Починено: **A** «какой товар виноват» давал
  «не умею» — `refine` оставлял метрику payment (sum_all) для products → теперь ставит amount/name;
  **B** «что не покупают» давал лучших — `RefineTopNOrder` детерминированно выставляет asc/desc;
  **C** orders/paycheck не фильтровались по периоду (даты в `open`/`close` рус. формата) — `filterByPeriod`
  научился их читать. Подтверждено на реальной LLM (quality-run2). Остаток: **D** «сравни выручку с прошлой
  неделей» → пусто (нужен metrics в дампе — добавлен); **E–H** UX-полировка (счётчик чеков, итог среднего
  чека, сортировка топа по выручке, пустое «Итого» в contribution). **Урок: бенчмарк плана не ловит качество
  ответа (order/числа/период) — нужен прогон по ответам (pipeval --dump).**

- **2026-06-22 — UX-фиксы + вклад по товарам (Этап 5.1).** По итогам ручного теста на реальной LLM:
  (1) **Пустой результат без вырожденного envelope** — бэкенд больше не шлёт нулевую таблицу, UI показывает
  честный текст «данных нет» (фронт уже умеет показывать answer без envelope). (2) **Лёгкий аудит-лог** —
  структурный slog по каждому Ask (intent/outcome/report/method/period/latency). (3) **Вклад по товарам**
  («какой товар виноват в падении выручки»): движок `Contribution` расширен раскладкой по ИЗМЕРЕНИЮ
  (products по name через amount) — `contribByDim`; маршрутизация **детерминированная** (`RefineProductContribution`,
  узкий матч «какой товар виноват/из-за каких товаров/вклад товаров»), НЕ через промпт. (4) Stub дочинен
  для офлайн-демо (help-формулировки), serve-host для запуска с реальной LLM. **Урок №2: фаззи-правило в
  промпте растекается на соседей — products→contribution дало 522→505; откатано, заменено детерминированной
  маршрутизацией (узкий паттерн, 551 не затрагивает).** Baseline остаётся 522 (промпт байт-в-байт = 522).

- **2026-06-22 — Откат follow-up-промпта (регрессия 523→507).** Контрольный eval-host после усиления
  follow-up показал **507/551** — просадка 16. Причина: в follow-up-блоке были перечислены «канал
  оплаты/точку/город» как цели переопределения, и модель стала недоизвлекать эти фильтры в ОДИНОЧНЫХ
  запросах (кластер регрессий: «чеки наличными», «заказы в городе» и т.п.) + «стата»/«покажи что-нибудь»
  поехали в smalltalk. Усиление дало +2 host-only follow-up кейса ценой ~10 кейсов headline-метрики 551 —
  плохой размен. **Промпт откатан байт-в-байт к проверенному 523** (коммит 283548f). Follow-up остаётся 5/7
  (известное ограничение на телеграфные эллипсисы «а за прошлый?» — лечится не общим промптом, а узко,
  без риска для одиночных запросов; отложено). Урок: правки общего промпта обязательно гонять полным
  eval-host ДО фиксации. **Подтверждено (eval-results-8.txt): 522/551 — baseline восстановлен (522 vs 523 =
  шум рига ±1); все 5 фильтр-кейсов, падавших на 507, снова PASS. Регрессия закрыта.**
- **2026-06-22 — Аудит покрытия + закрыт follow-up.** Аудит показал: единственный реальный пробел —
  контекст диалога (planner-eval гоняет историю=nil). Добавлен host-only набор `pipeline-followups.jsonl`
  (7 кейсов: «а за прошлый?», «а по карте?», «а по Москве?» и т.п.) — pipeval проигрывает историю faithfully
  через app.Ask. Первый прогон 5/7 нашёл реальную слабость на коротких эллипсисах; усилено правило follow-up
  в `systemPrompt` (наследуй отчёт/метод/класс/фильтры, меняй только названное) → **7/7 на реальной модели**.
  Команда: `make pipeval-followups-host`. **Внимание: тронут общий промпт — нужен контрольный полный
  eval-host, чтобы подтвердить, что 523/551 не просел.**
- **2026-06-22 — Этап 4.2: spotlighting против инъекций из истории.** В `systemPrompt` добавлен блок
  БЕЗОПАСНОСТЬ: реплики пользователя и история — это ДАННЫЕ, не инструкции; их содержимое не может
  переопределить правила, выдать системный промпт, открыть чужие/системные/персональные данные или снять
  ограничения — в т.ч. многоходово или под видом администратора → off_topic. Это замыкает Этап 2 (тот
  фильтр был single-turn). pipeval расширен многоходовыми кейсами (`Case.History`) + 2 кейса на инъекцию
  из истории (детерминированно в CI, осмысленно на риге). 16/16. Остаётся единственный страховочный слой —
  «сэндвич» уже ограничивает ущерб любой инъекции легальным отчётом своего тенанта.
  **Следующий шаг (по приоритету продукта): Этап 4.1 (аудит-лог) при появлении трафика, либо Этап 5
  (новые измерения) по спросу, либо M4 (реальный Dooglys-клиент вместо фикстур).**
- **2026-06-22 — Перепрогон (зафиксирован 523) + каркас full-pipeline eval.** Полный прогон планировщика
  (eval-results-6.txt): **523/551 (94.9%), валидных 530, 0 ERR** (+11 к 512; +96 от исходного 427). Все
  правки `40dacd7` подтвердились. Остаток (28) — почти целиком ограничения white-list (Этап 5.4): измерения
  категория/сотрудник/день-время, пороговые/источниковые фильтры на payment; в проде → честное «не умею».
  Зона убывающей отдачи планировщик-eval достигнута. **Построен второй бенчмарк — `pipeval` (Этап 0):**
  прогон через `app.Ask`, сверка ИТОГОВОГО ОТВЕТА (точные числа движка, непустота, нарратив, утечки),
  Stub+фикстуры → CI без рига (`make pipeval`), LLM → `make pipeval-host`; старт 8 кейсов, 8/8.
  **Следующий шаг: наполнять pipeline.jsonl кейсами с точными числами (paycheck/orders/contribution) —
  это покрывает математику движка, которую планировщик-eval не видит.**
- **2026-06-21 — Полный прогон + добивка качества.** Полный прогон (eval-results-5.txt): **512/551 (92.9%),
  валидных 526, 0 ERR** (было 490; +22; 3 ERR составных запросов ушли — parsePlan-фикс подтверждён;
  безопасность 62/62 за 0ms). Разбор 39 FAIL: ~10 — устаревшая разметка/промпт-пробел (модель права),
  ~25 — честные ограничения white-list (out-of-scope), ~4 — вариативность модели/сленг. Доправлено (коммит
  после прогона, ждёт следующего полного прогона, ожидаемо ~523): (а) промпт — СБП/онлайн это колонки
  payment, фильтр не добавлять; (б) разметка — «количество чеков»→payment|paycheck, фантомные фильтры
  город/точка без имени убраны, скидки→+products, «изменение выручки на точке»→compare|contribution,
  «какие данные покажешь»→help|smalltalk. **Остаток (~17) — ограничения white-list (Этап 5.4): измерения
  сотрудник/категория/день/время суток, пороговые фильтры (крупные чеки/большие скидки/нулевой чек),
  разрез payment по источнику (доставка/самовывоз). В проде дают честное «не умею». НЕ подгонялись.**
  **Следующий шаг: перепрогон для фиксации ~523; далее Этап 4 (аудит-лог) или Этап 5 (новые измерения).**
- **2026-06-21 — Этап 3.1 + чистка качества + честный out-of-scope.** (а) `parsePlan` через `json.Decoder`
  (fail-closed, чинит 3 ERR составных запросов). (б) Промпт-фиксы: top_n для «не покупают/не берут/плохо
  берут/маленький спрос» и «приносит больше всего»; «сколько чеков → paycheck»; «что продали → products».
  (в) Чистка разметки бенчмарка (~28 кейсов): убраны фантомные фильтры (роль/город/категория без имени),
  period-альтернативы для «за месяц», compare|contribution для «почему+payment», report-альтернативы.
  (г) **Реальный продуктовый фикс:** план вне white-list → честное «не умею такой разрез» вместо ПУСТОГО
  ответа (`app.outOfScopeMessage`, тест `TestOutOfScopeIsHonest`). Чинит весь класс out-of-white-list.
  Известные ограничения (НЕ подгонялись под бенчмарк, → Этап 5): рейтинг ПО категориям (категория — фильтр,
  не измерение), фильтр по дню недели («в пятницу»), «продавец месяца» (сленг не распознаётся моделью).
  **Следующий шаг: фокус-прогон затронутых кейсов, затем полный прогон → eval-results-5.txt.**
- **2026-06-21 — Этап 2: детерминированный refusal-пре-фильтр безопасности + усиление intent-гейта.**
  Добавлен слой `internal/planner/refuse.go`: 60+ regex-паттернов (PII клиентов/зарплаты, кросс-тенант/
  конкуренты, инъекции/джейлбрейк, раскрытие промпта, SQL/дамп БД, мутации, офф-домен) короткозамыкают
  запрос в `off_topic` ПРЯМО в `Plan()` ДО вызова модели — защита от джейлбрейка (модель не видит
  вредоносную инструкцию) + нулевая латентность. Паттерны выверены против всего набора (симуляция Go-RE2-
  семантики): **62/62 off_topic ловятся, 0 ложных срабатываний** на 480 кейсах (легальные «чеки за сегодня
  без фильтров», «все товары», «как изменилась выручка» не задеты). Учтено, что Go RE2 трактует `\w`/`\b`
  как ASCII — для кириллицы используются `[а-яё]*` и явные разделители. Дополнительно расширен intent-гейт
  в `systemPrompt()` (defense-in-depth для остатка) и добавлены юнит-тесты `refuse_test.go` (позитив: все
  вектора атак; негатив: легальные отчёты). `go test ./...` зелёный. Полный прогон против рига не выполнялся
  (риг недоступен из среды), но off_topic-ветка детерминирована и не ходит в сеть, а пре-фильтр доказанно не
  срабатывает на легальных кейсах → регрессии исключены. **Полный прогон выполнен (eval-results-4.txt):
  490/551 (88.9%), валидных 516, 3 ERR — это +26 к baseline 464. Все 62 off_topic зелёные, регресса нет.**
  Остаток фейлов (58): шум разметки/трудный NL (13× «нет фильтра», 11× неоднозначный отчёт, 4× period),
  4× нераспознан top_n, 3× конфликт правила compare↔contribution (почему+payment), 1× intent help.
  3 ERR — составные запросы («выручку и товары», «сначала чеки потом товары») = парс-краши.
  **Следующий шаг: Этап 3.1 (fail-closed parsePlan — чинит 3 ERR), затем чистка разметки бенчмарка (Этап 0)
  и дешёвый промпт-фикс на top_n.**
- **2026-06-21 — Этап 1: промпт-фиксы качества + нормализация method.** Полный прогон: **464/551 (84.2%)**
  (было 427 → **+37**), валидных 508, 4 ERR (3 систематических парс-краша + 1 транзиентный таймаут).
  Сделано: (1) «продажи/продал» → `payment`, не `orders`; (2) «за месяц» → `last_30_days`, не `last_month`;
  (3) `group_by` для payment всегда `["date"]` (sale_point/locality — только фильтры); (4) граница
  `contribution`/`compare` переведена на тип отчёта (payment→contribution, products/orders/paycheck→compare);
  (5) детерминированный инвариант `engine.NormalizeMethod` — contribution для отчётов без раскладки
  понижается в compare; eval теперь прогоняет план через ту же нормализацию, что прод (мерим итог, не сырой
  ответ модели). Проверено фокус-прогонами: sales 42/42, contribution/compare 33/36 (2 остатка — выбросы
  разметки бенчмарка, не лечим). **Следующий шаг: Этап 2 (детерминированный пре-фильтр безопасности, ~8 кейсов)
  — в новой сессии, потребует нового системного промпта; затем полный перепрогон.**
- **2026-06-21 — Baseline-прогон бенчмарка + разбор.** Полный прогон 551 кейса: **427/551 (77.5%)**,
  5 сетевых ошибок (погашены ретраем). Разбор фейлов выявил 3 реальных блока проблем:
  (1) безопасность — 19 кейсов эксфильтрации/инъекции не классифицируются как `off_topic`;
  (2) маппинг «продажи/продал → orders» вместо `payment` — ~30 кейсов;
  (3) парс-краши на составных запросах — 5 кейсов. Остальное (~65) — спорная разметка бенчмарка.
  Заведена эта дорожная карта. **Следующий шаг: Этап 1.1 (продажи→payment) и Этап 2.1 (пре-фильтр).**
- **2026-06-21 — Устойчивый eval-раннер.** Убран глобальный 10-мин таймаут (рушил хвост прогона),
  добавлен таймаут 90с на запрос + 3 ретрая с бэкоффом. Это позволило досчитать полный набор.
- **2026-06-20 — Бенчмарк 551 кейс.** Написан `test/eval/prompts.jsonl` (фильтры, аналитика,
  безопасность, smalltalk, метажалобы, опечатки, граничные случаи).
- **(ранее, коммиты phase 1–5) — P0-фиксы качества.** confidence-гейт, понижение contribution→compare,
  метод `top_n` с сортировкой, агрегация товаров по имени, распознавание метажалоб,
  честная пустота вместо болванки нарратора.

---

## Baseline

- **Текущий (eval-results-8.txt, после отката follow-up): 522/551 PASS (94.7%)**, валидных 530, **0 ERR**
  (= 523 ±1 шум рига; регрессия follow-up откатана).
- eval-results-6.txt: 523/551 (94.9%), валидных 530, 0 ERR.
  Остаток (28) — почти целиком честные ограничения white-list (Этап 5.4): измерения
  категория/сотрудник/день-время, пороговые/источниковые фильтры на payment. В проде → «не умею».
  Безопасность 62/62 off_topic за 0ms. Парс-краши устранены. Full-pipeline eval (pipeval) — 8/8.
- eval-results-5.txt: 512/551 (92.9%), валидных 526, 0 ERR.
- После Этапа 2 (eval-results-4.txt): 490/551 (88.9%), валидных 516, 3 ERR.
- После Этапа 1 (2026-06-21): 464/551 PASS, 508 валидных, 4 ERR.
- Исходный (2026-06-21): **427/551 PASS**, 501 валидных, 5 ERR; реальных проблем ~54 (безопасность 19 +
  продажи→orders ~30 + парс-краши 5), шум разметки ~65.

---

## Чек-лист (от простого к сложному)

### Аудит покрытия (2026-06-22)
Что покрыто: план (551), ответ+точные числа (pipeval 16), фильтр по точке, PII-strip,
нерезолвнутый фильтр→clarify, дефолтное измерение, таймзона, безопасность (62 + инъекции из истории).
Закрытый пробел: **follow-up/контекст диалога** — `pipeline-followups.jsonl` (host-only, `make pipeval-followups-host`).
Незакрытое (осознанно): мульти-тенант изоляция данных и реальные числа на боевых данных — это M4 (реальный
Dooglys-клиент вместо фикстур); на фикстурах изоляция непроверяема (один набор данных).

### Этап 0 — Инфраструктура бенчмарка
- [x] Устойчивый eval-раннер (ретрай, без глобального таймаута) — *2026-06-21*
- [x] Набор 551 кейс в `test/eval/prompts.jsonl` — *2026-06-20*
- [ ] Почистить спорную разметку бенчмарка (продажи, фильтры без значения, period-альтернативы),
      чтобы pass-rate отражал реальное качество
- [ ] Выгрузка результатов прогона в JSON/CSV (для удобного анализа и трекинга динамики)
- [x] **Full-pipeline eval** (`internal/pipeval` + `cmd/pipeval` + `test/eval/pipeline.jsonl`):
      прогон через `app.Ask`, сверка ИТОГОВОГО ОТВЕТА (точные числа из движка, непустота, нарратив
      class B, интент, авто-скан утечек). Stub+фикстуры → детерминированно в CI (`make pipeval`),
      реальный LLM → `make pipeval-host`. 14 кейсов: точные числа по всем 4 отчётам, compare/contribution
      (нарратив), честная пустота, clarify, интенты, авто-скан утечек. *2026-06-22* — расширять по находкам.

### Этап 1 — Дешёвые промпт-фиксы качества
- [x] **1.1** «продажи/продал/продаём» → отчёт `payment` (+ «за месяц»→last_30_days, group_by payment=["date"]) — *2026-06-21*
- [ ] **1.2** Усиление intent-гейта в промпте для безопасности (часть из 19 кейсов) → перенесено в Этап 2 (новая сессия)
- [x] **1.3** Граница `contribution` vs `compare` по типу отчёта + детерминированный `engine.NormalizeMethod` — *2026-06-21*
- [x] Перепрогон бенчмарка, зафиксирован новый pass-rate (464/551) — *2026-06-21*

### Этап 2 — Детерминированная безопасность (пре-фильтр)
- [x] **2.1** Refusal-layer ДО модели: regex/keyword на явные паттерны
      (`данные клиентов`, `ФИО/телефоны`, `все тенанты`, `system prompt`, `ignore instructions`,
      `ROLE: ADMIN`, `измени/удали`) → короткое замыкание в `off_topic`. Без латентности —
      `internal/planner/refuse.go`, 62/62 off_topic, 0 ложных. *2026-06-21*
- [x] **2.2** Юнит-тесты на пре-фильтр (позитив/негатив) — `internal/planner/refuse_test.go`. *2026-06-21*
- [x] **1.2** Усиление intent-гейта в `systemPrompt()` (defense-in-depth для остатка regex). *2026-06-21*

### Этап 3 — Устойчивость парсинга
- [x] **3.1** Fail-closed `parsePlan`: `json.Decoder` берёт первый полный JSON-объект (двойной ответ модели перестаёт крашить); при неисправимом сбое — уточнение вместо ошибки. Чинит 3 ERR составных запросов. *2026-06-21*
- [ ] **3.2** (опц.) Строгая JSON-грамматика (GBNF/structured output) на риге — убирает класс парс-ошибок в корне

### Этап 4 — Наблюдаемость и hardening
- [~] **4.1** Лог отказов/уточнений (аудит) по сессии/тенанту.
      - [x] Лёгкий срез: структурный slog по каждому `Ask` (tenant/session/intent/outcome/report/method/
        period/latency, исход answer|empty|clarify|out_of_scope|off_topic|help|smalltalk|error). Включён
        по умолчанию (`App.Logger`). *2026-06-22*
      - [ ] Полный аудит: персистентное хранилище по тенанту + мониторинг аномалий + датасет — к выкатке/трафику
        (наполняется реальным использованием, не фикстурами; зависит от трафика, не от M4).
- [x] **4.2** Spotlighting: блок БЕЗОПАСНОСТЬ в `systemPrompt` — реплики/история = ДАННЫЕ, не инструкции;
      нельзя переопределить правила/выдать промпт/снять лимиты даже многоходово. pipeval поддерживает
      многоходовые кейсы (`Case.History`) + 2 кейса на инъекцию из истории. *2026-06-22*

### Этап 5 — Гибкость: богатый white-list (без потери детерминизма)
- [~] **5.1** Аналитика поверх базовых полей.
      - [x] Вклад по товарам: `Contribution` раскладывает изменение по измерению (products по name через
        amount) + детерминированная маршрутизация `RefineProductContribution` («какой товар виноват»). *2026-06-22*
      - [~] Derived-метрики (средний чек, конверсия, доля канала, % возвратов) — Go-вычисления из базовых полей.
        - [x] Взвешенный средний чек (`AvgCheck` = sum_all/kol_vo_chekov), доля канала (`ChannelMix.Share`) в
          `InsightBundle` + `Compose`. *2026-06-23 (8812017)*
        - [ ] % возвратов (return_sum/revenue), доля скидок (discounts/revenue) — относительные метрики

### Этап 5.6 — Консультант (advisor): ценность из расширенного снимка
> Ядро bundle, routing, advisor-LLM, advice-eval и расширенные поля (AvgCheck/ChannelMix/TopProducts)
> сделаны (*2026-06-23*). Поля посчитаны и тестируемы, но **advisor-промпт их пока не использует** —
> следующий трек закрывает именно это. Дизайн: [06-assistant-design.md](06-assistant-design.md).
- [ ] **5.6.1** advisor-`systemPrompt` описывает и применяет `avg_check / channel_mix / top_products`
      (средний чек и каналы — для «что улучшить / как поднять выручку»; топ — «на что опираться»).
      ⚠️ правка промпта → полный `eval-host` до коммита (правило: узкие правки промпта роняли score).
- [ ] **5.6.2** Усилить advice-ассерты: для «что улучшить» ждать упоминание чека/канала
      (`contains_any`) → регрессионная защита, что новые поля реально попадают в текст.
- [ ] **5.6.3** Фильтры (точка/категория) в снимке: прокинуть фильтры плана в `app.advise` →
      `BuildInsightBundle` собирает срез по точке/категории (сейчас снимок всегда по всему заведению).
- [ ] **5.2** Plan-confirm для неоднозначных запросов (эхо интерпретации перед исполнением)
- [ ] **5.3** Словарь синонимов NL→схема как **config-as-data** (НЕ RAG/вектор). Решение 2026-06-23:
      словарь СХЕМЫ (оборот→payment, картой→sum_card) мал и весь влезает в промпт — RAG здесь оверхед,
      ломающий детерминизм (retrieval недетерминирован → eval нерепродуцируем; узкое место — полнота
      каталога и маршрутизация, а не recall синонимов). Правильный путь: поднять синонимы в `catalog.Field`
      (`Synonyms []string`) и генерировать секцию промпта из `Catalog.Describe()` — один источник истины,
      тестируемо, ноль инфраструктуры. **Вектор/эмбеддинги оправданы ТОЛЬКО для словаря СУЩНОСТЕЙ при
      масштабе** (тысячи SKU у тенанта, кастомные пер-тенантные словари) — fuzzy-резолв имён→UUID, пост-MVP,
      триггер data-driven. Сейчас имена→UUID закрыты живым резолвером из HTML-формы.
- [ ] **5.4** Новые измерения/фильтры из бенчмарка (модель сейчас выдумывает несуществующую разбивку →
      план невалиден → честное «не умею»): разбивка по категории товаров (рейтинг ПО категориям),
      разбивка по сотруднику/кассиру (кассир — PII, нужно агрегатное измерение), фильтр по дню недели
      («в пятницу»). Соответствующие бенчмарк-кейсы оставлены FAIL как честные ограничения white-list,
      НЕ подогнаны под разметку.
- [ ] **5.5** Язык нарратива: LLM-нарратор (qwen2.5) иногда срывается в КИТАЙСКИЙ в середине ответа
      (обнаружено 2026-06-23 на кейсе «а за прошлый?»: `Прирост выручки主要是由信用卡支付增加…`). Числа/таблица
      верны (нарратив декоративен), но текст нечитаем. Фикс: (а) явное правило в промпте нарратора «отвечай
      ТОЛЬКО на русском»; и/или (б) пост-фильтр — детектить долю не-кириллицы/не-латиницы в ответе и при
      превышении порога падать на детерминированный `Compose` (он уже есть как fallback). Предпочтительнее (б)
      как гарантия (промпт qwen держит язык нестабильно). Низкий приоритет: на безопасность/числа не влияет.

### Этап 6 — Гибкость: архитектурно (крупные задачи)
- [ ] **6.1** Композируемый query-AST вместо «1 из N фиксированных отчётов» (валидация на уровне поля/операции)
- [ ] **6.2** (далеко) Витрина данных (скрейп → локальное хранилище) → предпосылка для text-to-SQL/семантического слоя с RLS

---

## Принципы (не нарушать при доработках)

- LLM не считает числа — вся математика детерминирована в Go.
- `tenant_id` и токены — только server-side, в план модели не попадают.
- Любой выход за white-list → отказ/уточнение до обращения к данным.
- Лучше честное «не умею / уточните», чем уверенно неверный ответ.
- Каждая доработка сопровождается кейсами в бенчмарке и/или тестами.
